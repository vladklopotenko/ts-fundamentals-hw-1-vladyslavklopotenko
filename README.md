# **ДЗ №1 — Library System (ООП + TypeScript)**

---

## 🎯 Мета

Закріпити класи, керування станом і помилками, використання власних типів.

---

## 🧩 Як створити власний репозиторій із цього шаблону

Цей проєкт — **template repository**. Ти не працюєш безпосередньо в цьому репо, а створиш **свій власний** на його основі.

### 🔧 Кроки

1. Відкрий сторінку цього репозиторію на GitHub.
2. Угорі натисни **Use this template → Create a new repository**.
3. У полі **Repository name** введи свою назву, наприклад:

```text
ts-fundamentals-hw-1-yourname
```

4. Переконайся, що репозиторій створюється **власний (на твоєму акаунті)**.
5. Натисни **Create repository**.
6. Після створення:

   - відкрий свій новий репозиторій;
   - натисни **Code → Clone** і скопіюйте SSH або HTTPS посилання;
   - склонуй проєкт собі на комп'ютер:

     ```bash
     git clone <посилання>
     ```

   - відкрий папку у VS Code;
   - встанови залежності:

     ```bash
     npm install
     ```

   - запустіть:

     ```bash
     npm run dev
     ```

Після цього ти зможеш писати код, бачити результати тестів і пушити зміни у свій GitHub.

---

## 🧪 Перевірка коду та автотести

У цьому проєкті вже налаштовані **автоматичні тести** та **перевірка типів TypeScript**.
Тобі не потрібно нічого додатково встановлювати або змінювати — просто працюй з кодом.

### ▶️ Як запустити перевірку

1. Встанови залежності (один раз після клонування репозиторію):

   ```bash
   npm install
   ```

2. Запусти режим розробки:

   ```bash
   npm run dev
   ```

### ⚙️ Що відбувається під час `npm run dev`

У цьому режимі одночасно працюють:

- **тести (Vitest)** — перевіряють, чи правильно реалізовано класи `Book` і `Library`;
- **перевірка типів (TypeScript)** — стежить, щоб усі типи відповідали ТЗ.

Після кожного збереження файлу:

- тести автоматично перезапускаються;
- у терміналі з'являються результати (зелено ✅ — усе добре, червоно ❌ — треба виправити).

### ✅ Перед здачею роботи переконайся, що:

- усі тести проходять успішно (усі зелені ✅);
- у терміналі немає TypeScript-помилок;
- класи, методи, типи й повідомлення помилок відповідають технічному завданню;
- структура файлів не змінена.

### ☁️ Автоматична перевірка на GitHub

Після завантаження твоєї роботи у репозиторій (через **push** або **pull request**)
GitHub автоматично запустить тести та перевірку типів.

Якщо все правильно — перевірка пройде успішно ✅.
Якщо ні — у вкладці **Actions** ви побачите червоний ❌
та зможете відкрити **Logs**, щоб дізнатися, що саме не так.

---

## 🧩 Що реалізувати

Дві сутності — `Book` і `Library` — з можливістю:

- додавати та видаляти книги;
- позичати та повертати книги конкретній людині;
- переглядати всі книги й лише доступні;
- отримувати читабельний опис книги у вигляді рядка.

---

## 🚫 Обмеження

- Лише вбудовані можливості TypeScript/JS (без сторонніх бібліотек).
- Помилки викликаються через `throw new Error("<точний текст>")`.
- Будь-який стан зберігається **всередині** класів, без глобальних змінних.

---

## 📁 Структура файлів

```text
src/
 ├── types.ts
 ├── book.ts
 ├── library.ts
 └── main.ts
```

---

## 🔠 Типи даних

У `src/types.ts` оголоси й використовуй у класах такі типи:

- `BookId` — рядок;
- `Genre` — один із літералів: `"fiction" | "science" | "history" | "fantasy" | "dystopian"`;
- `LoanStatus` — `"available" | "borrowed"`.

---

## 📚 Вимоги до класу `Book`

### Публічні властивості (тільки описові поля)

- `id: BookId`
- `title: string`
- `author: string`
- `year: number`
- `genre: Genre`

### Приватні дані (не доступні зовні)

- `status: LoanStatus` — за замовчуванням `"available"`;
- `borrowedBy: string | null` — за замовчуванням `null`.

### Поведінка методів Book

- **`getStatus()`** — повертає поточний статус.
- **`markBorrowed(personName: string)`** —
  встановлює статус `"borrowed"` і зберігає `personName` у `borrowedBy`.
  Якщо книга вже позичена — кидає помилку
  `Already borrowed by <name>`, де `<name>` — поточний позичальник.
- **`markReturned()`** —
  переводить статус у `"available"` і скидає `borrowedBy` в `null`.
  Якщо книга вже доступна — кидає помилку `Already available`.
- **`getInfo()`** —
  повертає короткий опис книги:

  - для доступної: `Title — Author (Year), genre [Available]`;
  - для позиченої: `Title — Author (Year), genre [Borrowed by <name>]`.

> Важливо: `borrowedBy` має використовуватися у статусі, помилках і описі книги.

---

## 🏛️ Вимоги до класу `Library`

### Зберігання

Колекція книжок за `BookId` (рекомендовано `Map<BookId, Book>`).
Стан має бути інкапсульований.

### Поведінка методів Library

- **`add(item: Book)`** — додає книгу;
  якщо `id` вже існує — кидає помилку `Item already exists`.
- **`remove(id: BookId)`** —
  якщо книги не існує — `Book not found`;
  якщо книга позичена — `Cannot remove borrowed item`;
  інакше — видаляє книгу.
- **`listAll()`** — повертає всі книги.
- **`listAvailable()`** — повертає лише ті, у яких статус `"available"`.
- **`borrow(bookId: BookId, personName: string)`** —
  якщо книги не існує — `Book not found`;
  інакше викликає `markBorrowed` книги (делегує помилку `Already borrowed by <name>`).
- **`return(bookId: BookId)`** —
  якщо книги не існує — `Book not found`;
  інакше викликає `markReturned` книги (делегує помилку `Already available`).

> Можна створити внутрішній метод `getBookOrThrow(id: BookId)`, який кидає `Book not found`, якщо книга не знайдена.

---

## ⚠️ Точні тексти помилок

- `Item already exists`
- `Cannot remove borrowed item`
- `Book not found`
- `Already borrowed by <name>`
- `Already available`

---

## 🧾 Формат рядка, який повертає `getInfo()`

- Доступна книга:
  `Title — Author (Year), genre [Available]`
- Позичена книга:
  `Title — Author (Year), genre [Borrowed by <name>]`

---

## 🧩 Демо-сценарій у `main.ts`

Файл `src/main.ts` уже містить готовий **демо-сценарій**, який показує, як працюють ваші класи `Book` і `Library`.

У консолі має з'явитися послідовність дій:

1. Створюються кілька книжок різних жанрів.
2. Вони додаються до бібліотеки.
3. Виводиться список усіх книжок (`getInfo()`).
4. Одна книга позичається конкретній людині.
5. Список оновлюється й показує статус «Borrowed by …».
6. Книга повертається — статус змінюється на «Available».
7. Одна книга видаляється, і виводиться фінальний список.

> ⚙️ Цей сценарій разом із автотестами допоможе тобі швидко перевірити, що класи працюють правильно.

---

## 🚫 Заборонено / ✅ Дозволено

**Заборонено:**

- дженерики;
- сторонні бібліотеки;
- глобальні змінні для стану.

**Дозволено:**

- `Map`, `Set`, `Array`;
- внутрішні допоміжні функції без зміни публічного API.

---

## ✅ Чекліст для студента

Перед здачею перевір:

- Є всі необхідні файли:
  `src/types.ts`, `src/book.ts`, `src/library.ts`, `src/main.ts`.
- Типи `BookId`, `Genre`, `LoanStatus` оголошені та використані у класах.
- У класі `Book`:
  - поля `status` і `borrowedBy` інкапсульовані;
  - повторна позика тієї ж книги → помилка `Already borrowed by <name>`;
  - повторне повернення доступної → помилка `Already available`;
  - метод `getInfo()` форматує рядок згідно шаблону.
- У класі `Library`:
  - неіснуючі `id` → помилка `Book not found`;
  - дублікати → `Item already exists`;
  - видалення позиченої → `Cannot remove borrowed item`;
  - методи `listAll()` і `listAvailable()` повертають правильні списки.
- Демо-сценарій у `main.ts` показує роботу всіх методів.

---

## 🧮 Критерії прийому

> Тести та ментор оцінюють саме ці аспекти:

- ✅ Дотримано структури проєкту та назв файлів.
- ✅ Типи з `types.ts` правильно імпортовані та застосовані.
- ✅ У публічному API немає `any`.
- ✅ Логіка станів (`available` ↔ `borrowed`) працює коректно.
- ✅ `borrowedBy` використовується в усіх потрібних місцях:
  при позиці, поверненні, в описі книги та у тексті помилки.
- ✅ Точні тексти помилок і формат рядка з `getInfo()` збігаються з вимогами.
- ✅ Демо у `main.ts` виконується без помилок і відображає правильні результати.
